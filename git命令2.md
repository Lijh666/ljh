```git
mkdir +文件夹名(创建一个文件夹)
cd +文件夹名（移动到这个位置）
git config --global user.name "ljh66666"
git config --global user.email "2396920525@qq.com"(配置用户名和邮箱)
git config --list（显示版本信息）
git init
git clone +仓库网址
cd +文件夹名
git add +文件名
git commit -m"create a new txt name is +文件名"
git push
git mv 1.txt 111.txt
git commit -m "rename is 111.txt"
git push
git rm 111.txt
git commit -m"delete 111.txt"
git push







```

